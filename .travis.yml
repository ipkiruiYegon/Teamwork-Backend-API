language: node_js
node_js:
  - 'stable'
services:
  - postgresql
before_script:
  - psql -c 'create database test_db;' -U postgres
script: npm run travis build
after_success: npm test
cache:
  directories:
    - 'node_modules'
